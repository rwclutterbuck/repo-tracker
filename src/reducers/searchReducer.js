const initState = {
  gitUser: "",
  result: {},
  loading: false,
};

const searchReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default searchReducer;
